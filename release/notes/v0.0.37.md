**Smart networking — DNS that survives broken VPN/TUN**

- `SmartResolver`: system resolver first, transparent DNS-over-HTTPS bootstrap via `cloudflare-dns.com` (pinned anycast IPs) when DNS fails or times out — works under any VPN, proxy, or none at all
- Coalesced network status: one clear status line instead of a retry storm; single retry core for `remote_len`; anti-spam `goenv` output
- TUI: `NET ⚠` warning bar on the deployment screen
- README: source-available license memo for people forking the repo
- 150 tests passing, clippy clean