**Sandbox hardening**

- Runtime units drop the ambient session: D-Bus, X11 and user-runtime sockets are masked, `@debug` is denied, and `UnsetEnvironment` carries the sandbox scrub plus cloud/db credentials
- All three catalog services pinned to loopback-only binds