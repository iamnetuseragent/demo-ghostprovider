**Live cross-window logs & network resilience**

- Shared `deploy.log` tailed live by every window; per-unit journal poll for Software logs; `[d]` clears logs with cross-window propagation
- Network resilience: infinite retry with exponential backoff for all fetches + best-effort Go module seed
- `demo-test` gets a red/black concept TUI