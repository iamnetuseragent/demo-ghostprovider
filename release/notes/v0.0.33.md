**Offline-friendly sandbox build**

- Pre-seeded Go toolchain now skips the sumdb (`GONOSUMDB`) — the offline sandbox build completes without network access
- My Services actions actually work end-to-end: `[s]`top, star`[t]`, `[d]`elete, `[r]`efresh — title row and hint bar agree